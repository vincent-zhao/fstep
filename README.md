fstep
====

A light node project framework

### Install

```
npm install -g fstep
```

### Usage

* edit config file named `config.ini` like `config-example.ini`
* run command below

```
fstep config.ini
```

